//
//  ButtonColors+Extension.swift
//  Schedl
//
//  Created by David Medina on 6/28/25.
//

import UIKit
import SwiftUI

extension ButtonColors {
    static let palette1 = ButtonColors(
        backgroundColor: UIColor(Color(hex: 0xCBACCE)), // Light lavender
        foregroundColor: UIColor(Color(hex: 0x4A2C54))  // Deep purple
    )
    
    static let palette2 = ButtonColors(
        backgroundColor: UIColor(Color(hex: 0x9BC4C7)), // Soft teal
        foregroundColor: UIColor(Color(hex: 0x2C5B5F))  // Dark teal
    )
    
    static let palette3 = ButtonColors(
        backgroundColor: UIColor(Color(hex: 0x7BA088)), // Sage green
        foregroundColor: UIColor(Color(hex: 0x3A4D40))  // Forest green
    )
    
    static let palette4 = ButtonColors(
        backgroundColor: UIColor(Color(hex: 0x6D5474)), // Muted purple
        foregroundColor: UIColor(Color(hex: 0xE8D5EA))  // Light lavender
    )
}
